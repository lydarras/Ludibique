{% extends 'base.html.twig' %}

{% block title %}Ludibique{% endblock %}

{% block body %}
    <div class="accueil">
        <div class="gauche">
            <div class="contenu bienvenue">
                <h2>Bienvenue à Ludibique</h2>
                <div class="kesako">
                    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam libero nisi, suscipit egestas pharetra eget, tristique elementum odio. Sed suscipit mauris non neque posuere cursus. Praesent hendrerit iaculis molestie.
                </div>
            </div>
        </div>
        
        <div class="droite">
            <div class="contenu seanceAleatoire">
                <h2>Les 5 derniers séances crées</h2>
                    <div class="seanceVirtuelRandom">

                    {% for seance in seances %}
                        <div class="uneSeancevirtuelle">
                            <div class="miniatureJeu"></div>
                                <p>{{ seance.jeu.nom }}</p>
                                <p>{{ seance.nom }}</p>
                                <p>{{ seance.organisateur.pseudo }}</p>
                                <p><a href="{{ path('app_seance_show', {'id': seance.id}) }}">Afficher les détails</a></p>
                                {% if app.user %}
                                    {% if seance in participation %}
                                        <p><a href="{{ path('supprimerJoueur', {'id': seance.id}) }}">Se retirer</a></p>
                                    {% else %}
                                        <p><a href="{{ path('ajouterJoueur', {'id': seance.id}) }}">Participer</a></p> 
                                    {% endif %}
                                {% endif %}
                                        
                        </div>
                    {% endfor %}

                        <button>Afficher toutes les séances virtuelles</button>

                    </div>

                </div>
            </div>

        </div>


    </div>
{% endblock %}